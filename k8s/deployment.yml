apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-boot-FoodApp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-boot-FoodApp
  template:
    metadata:
      labels:
        app: spring-boot-FoodApp
    spec:
      containers:
        - name: spring-boot-FoodApp
          image: docker.io/shankham/spring-boot-FoodApp:latest  
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
